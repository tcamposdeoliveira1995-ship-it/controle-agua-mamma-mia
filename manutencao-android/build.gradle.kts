// Arquivo raiz do projeto — declara os plugins usados pelo módulo app,
// sem aplicá-los aqui (apply false). Nada pra mexer neste arquivo.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
